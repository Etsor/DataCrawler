import logging

logging.basicConfig(
                    level="INFO",
                    filename="logs.log",
                    filemode="w", 
                    format="%(asctime)s %(levelname)s %(message)s")
